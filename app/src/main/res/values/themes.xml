<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ClipboardShare" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent theme so app shows no window, just a Toast -->
    <style name="Theme.ClipboardShare.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
